Installation
============

This installation manual will cover the following systems:

.. toctree::
   :maxdepth: 1

    Windows with XAMPP   <installation/install_windows_xampp.rst>


Although the software is quite generic an can run on many other systems, we cannot test every
OS and web server combination. So we only test the ones we use ourselves.

If you have it running on a specific OS and web server combination, write an installation manual
and we can add it here.
